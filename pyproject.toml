[project]
name = "stringhide"
version = "0.1.2"
dependencies = []
readme = "README.md"
description = "A Python module to encrypt strings with different algerithms."

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
